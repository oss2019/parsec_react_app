import React from 'react';
import Header from '../../components/Header';

const Schedule = () => {
	return (
		<React.Fragment>
			<Header active={4} />
			<h1>Schedule</h1>
		</React.Fragment>
	);
};

export default Schedule;
